[Service]
# The stock unit runs `nft flush ruleset` on stop, which would also remove
# NetworkManager's shared-mode tables and break a running hotspot. Remove
# only the table /etc/nftables.conf owns.
ExecStop=
ExecStop=/usr/sbin/nft destroy table inet pms
