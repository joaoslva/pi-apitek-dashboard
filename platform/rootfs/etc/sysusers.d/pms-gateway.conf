# The gateway runs unprivileged as this user. Its privileged actions are
# limited by /etc/polkit-1/rules.d/50-pms-gateway.rules.
#Type Name        ID GECOS                      Home directory         Shell
u     pms-gateway -  "pi-mobile-server gateway" /var/lib/pms-gateway   /usr/sbin/nologin
